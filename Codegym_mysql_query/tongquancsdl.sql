CREATE DATABASE `c0225g1` ;

use c0225g1;

create table Class(
id int primary key,
`name` varchar(30)
);

create table teacher(
id int primary key,
`name` varchar(30),
age int,
country varchar(30)
);

